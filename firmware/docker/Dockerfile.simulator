# MQTT Simulator Dockerfile
FROM python:3.11-slim

WORKDIR /app

# Copy simulator script (must exist in build context)
COPY simulator.py ./

# Install MQTT client library
RUN pip install --no-cache-dir paho-mqtt

CMD ["python", "simulator.py"]
